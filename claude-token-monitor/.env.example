# Claude API Configuration
# Multiple authentication methods are supported (in order of priority):

# Method 1: Claude CLI OAuth (Recommended)
# Install: npm install -g @anthropic-ai/claude-cli
# Login: claude auth login
# Credentials stored in: ~/.claude/.credentials.json

# Method 2: Direct API Key Environment Variables
# Get your API key from: https://console.anthropic.com/
CLAUDE_API_KEY=your_api_key_here
# Or alternatively:
ANTHROPIC_API_KEY=your_api_key_here

# Method 3: Command line flag
# cargo run -- --api-key your_key_here monitor

# Optional: Custom API base URL (default: https://api.anthropic.com)
# CLAUDE_API_BASE_URL=https://api.anthropic.com

# Optional: API timeout in seconds (default: 30)
# CLAUDE_API_TIMEOUT=30

# Optional: Number of retry attempts (default: 3)
# CLAUDE_API_RETRIES=3

# To check authentication status:
# cargo run -- auth status